import { LibrarySearchCriteriaFilterPipe } from './library-search-criteria-filter.pipe';

describe('LibraryGenreFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LibrarySearchCriteriaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
