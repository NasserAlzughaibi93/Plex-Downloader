import { LibraryGenreFilterPipe } from './library-genre-filter.pipe';

describe('LibraryGenreFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LibraryGenreFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
