import { LibrarySeriesSearchCriteriaFilterPipe } from './library-series-search-criteria-filter.pipe';

describe('LibrarySeriesGenreFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LibrarySeriesSearchCriteriaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
