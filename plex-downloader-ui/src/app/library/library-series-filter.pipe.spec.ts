import { LibrarySeriesFilterPipe } from './library-series-filter.pipe';

describe('LibrarySeriesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LibrarySeriesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
